#include <vector>

float reference_solution(const std::vector<float>& a, const std::vector<float>& b);
void precompile();
float solution(const std::vector<float>& a, const std::vector<float>& b, bool profile = false);
